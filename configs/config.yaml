# Configuration file
server:
  port: 8080
  host: "0.0.0.0"

database:
  host: "localhost"
  port: 5432
  user: "user"
  password: "password"
  dbname: "reservasi"

redis:
  host: "localhost"
  port: 6379

graphql:
  playground: true